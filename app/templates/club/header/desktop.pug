include ../container/desktop
include ../logo/desktop
include ../search-form/desktop

mixin header()
  +b('header').xfo-header
    +container()
      +e.top

        +e.address
          | Санкт-Петербург
          = ' '
          +e('span').address-abbr (...)
          = ' '
          | д. 5

        +e('ul').top-menu
          +e('li').top-menu-item
            +e('a')(href='#').top-menu-link
              | Магазины
          +e('li').top-menu-item
            +e('a')(href='#').top-menu-link
              | Помощь
          +e('li').top-menu-item
            +e('a')(href='#').top-menu-link
              | Доставка и оплата
          +e('li').top-menu-item
            +e('a')(href='#').top-menu-link
              | Статус заказа

        +e('ul').phones
          +e('li').phones-item
            +e('span').phones-title
              | Интернет-магазин, 24/7
            +e('a')(href='tel:84955554545').phones-phone
              | 8-495-555-45-45

          +e('li').phones-item
            +e('span').phones-title
              | Справочная служба
            +e('a')(href='tel:88002009555').phones-phone
              | 8-800-200-95-55

      +e.middle
        +e.logo
          +logo()

        +e.search-form
          +search-form()

        +e.cart-widget
          +e('a')(href='#').cart-widget-item._favorites
            +e('span').cart-widget-icon._favorites
            +e('span').cart-widget-count
              | 3

          +e('a')(href='#').cart-widget-item._favorites
            +e('span').cart-widget-icon._cart
            +e('span').cart-widget-count
              | 7

          +e('span').cart-widget-price
            | 15 629
            +e('span').cart-widget-price-kop
              | 90

      +e.bottom
        +e.catalog.jso-header__catalog
          +e.catalog-button
            +e('span').catalog-button-title
              | Каталог товаров
            +e('span').catalog-button-burger
          +e.catalog-content.jso-header__catalog-content
            +e('ul').catalog-items
              +e('li').catalog-item
                +e('a')(href='#').catalog-link._next
                  | Ссылка #1

                +e('ul').catalog-items._next
                  +e('li').catalog-item
                    +e('a')(href='#').catalog-link
                      | Ссылка #1.1

                  +e('li').catalog-item
                    +e('a')(href='#').catalog-link._next
                      | Ссылка #1.2

                    +e('ul').catalog-items._next
                      +e('li').catalog-item
                        +e('a')(href='#').catalog-link
                          | Ссылка #1.2.1

                      +e('li').catalog-item
                        +e('a')(href='#').catalog-link
                          | Ссылка #1.2.2

                  +e('li').catalog-item
                    +e('a')(href='#').catalog-link
                      | Ссылка #1.3

                  +e('li').catalog-item
                    +e('a')(href='#').catalog-link
                      | Ссылка #1.4

                  +e('li').catalog-item
                    +e('a')(href='#').catalog-link
                      | Ссылка #1.5

              +e('li').catalog-item
                +e('a')(href='#').catalog-link
                  | Ссылка #2

              +e('li').catalog-item
                +e('a')(href='#').catalog-link
                  | Ссылка #3

              +e('li').catalog-item
                +e('a')(href='#').catalog-link
                  | Ссылка #4

              +e('li').catalog-item
                +e('a')(href='#').catalog-link
                  | Ссылка #5

              +e('li').catalog-item
                +e('a')(href='#').catalog-link
                  | Ссылка #6

              +e('li').catalog-item
                +e('a')(href='#').catalog-link
                  | Ссылка #7

              +e('li').catalog-item
                +e('a')(href='#').catalog-link
                  | Ссылка #8

              +e('li').catalog-item
                +e('a')(href='#').catalog-link
                  | Ссылка #9

              +e('li').catalog-item
                +e('a')(href='#').catalog-link
                  | Ссылка #10

        +e('ul').bottom-menu
            +e('li').bottom-menu-item
              +e('a')(href='#').bottom-menu-link
                | Акции и скидки

            +e('li').bottom-menu-item
              +e('a')(href='#').bottom-menu-link
                | Клуб перекресток

            +e('li').bottom-menu-item._dropdown
              +e('a')(href='#').bottom-menu-link._dropdown
                | О Компании

              +e('ul').bottom-menu-dropdown-items
                +e('li').bottom-menu-dropdown-item
                  +e('a')(href='#').bottom-menu-dropdown-link
                    | Ссылка #1

                +e('li').bottom-menu-dropdown-item
                  +e('a')(href='#').bottom-menu-dropdown-link
                    | Ссылка #1

                +e('li').bottom-menu-dropdown-item
                  +e('a')(href='#').bottom-menu-dropdown-link
                    | Ссылка #1

                +e('li').bottom-menu-dropdown-item
                  +e('a')(href='#').bottom-menu-dropdown-link
                    | Ссылка #1

                +e('li').bottom-menu-dropdown-item
                  +e('a')(href='#').bottom-menu-dropdown-link
                    | Ссылка #1

                +e('li').bottom-menu-dropdown-item
                  +e('a')(href='#').bottom-menu-dropdown-link
                    | Ссылка #1

        +e('ul').user-menu
          +e('li').user-menu-item
            +e('a')(href='#').user-menu-link
              | Вход
          +e('li').user-menu-item
            +e('a')(href='#').user-menu-link
              | Регистрация
